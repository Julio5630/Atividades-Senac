#include <stdio.h>  // Inclui a biblioteca padrão

int main() {        // Função principal
  printf("Maçã, Banana, Laranja, Uva, Morango\n");  // Imprime a lista de frutas separadas por vírgulas
  return 0;         // Retorna 0 
}
